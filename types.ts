
export interface LoveData {
  id: string;
  slug: string;
  yourName: string;
  partnerName: string;
  message: string;
  images: string[];
  date: string;
  pin?: string;
  song: string;
}

export type Language = 'en'; // Simplified to single language
